==========================================================================================
저장한 시간 - 2026-05-11 13:39:37.2035047
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::cout << "데이터 읽는 중";
   std::list<ZString> words{ std::istream_iterator<ZString>{in}, {} };
   std::cout << std::endl;

   std::cout << "정렬하는 중";
   words.sort([](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });
   std::cout << std::endl;

   for (const ZString& zs : words)
      std::cout << zs << std::endl;

   std::cout << "단어의 수 - " << words.size() << std::endl;
   save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-05-11 13:42:58.3904475
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include <chrono>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::cout << "데이터 읽는 중";
   std::list<ZString> words{ std::istream_iterator<ZString>{in}, {} };
   std::cout << std::endl;

   std::cout << "정렬하는 중";
   auto b = std::chrono::high_resolution_clock::now();
   words.sort([](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });
   auto e = std::chrono::high_resolution_clock::now();
   std::cout << "걸린시간 - " << std::chrono::duration_cast<std::chrono::milliseconds>(e - b) << std::endl;

   std::cout << std::endl;

   //for (const ZString& zs : words)
   //   std::cout << zs << std::endl;

   std::cout << "단어의 수 - " << words.size() << std::endl;
   save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-05-11 13:43:10.4873246
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include <chrono>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::cout << "데이터 읽는 중";
   std::list<ZString> words{ std::istream_iterator<ZString>{in}, {} };
   std::cout << std::endl;

   std::cout << "정렬하는 중";
   auto b = std::chrono::high_resolution_clock::now();
   words.sort([](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });
   auto e = std::chrono::high_resolution_clock::now();
   std::cout << "걸린시간 - " << std::chrono::duration_cast<std::chrono::microseconds>(e - b) << std::endl;

   std::cout << std::endl;

   //for (const ZString& zs : words)
   //   std::cout << zs << std::endl;

   std::cout << "단어의 수 - " << words.size() << std::endl;
   save("메인.cpp");
}

==========================================================================================
저장한 시간 - 2026-05-11 13:45:00.1891940
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include <chrono>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::cout << "데이터 읽는 중";
   std::vector<ZString> words{ std::istream_iterator<ZString>{in}, {} };
   std::cout << std::endl;

   std::cout << "정렬하는 중";
   auto b = std::chrono::high_resolution_clock::now();
   std::sort(words.begin(), words.end(), [](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });
   auto e = std::chrono::high_resolution_clock::now();
   std::cout << "걸린시간 - " << std::chrono::duration_cast<std::chrono::microseconds>(e - b) << std::endl;

   std::cout << std::endl;

   //for (const ZString& zs : words)
   //   std::cout << zs << std::endl;

   std::cout << "단어의 수 - " << words.size() << std::endl;
   save("메인.cpp");

   // 리스트 정렬시간 5476 us
}

==========================================================================================
저장한 시간 - 2026-05-11 13:46:50.4850706
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include <chrono>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::cout << "데이터 읽는 중";
   std::vector<ZString> words{ std::istream_iterator<ZString>{in}, {} };
   std::cout << std::endl;

   std::cout << "정렬하는 중";
   auto b = std::chrono::high_resolution_clock::now();
   std::sort(words.begin(), words.end(), [](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });
   auto e = std::chrono::high_resolution_clock::now();
   std::cout << "걸린시간 - " << std::chrono::duration_cast<std::chrono::microseconds>(e - b) << std::endl;

   std::cout << std::endl;

   //for (const ZString& zs : words)
   //   std::cout << zs << std::endl;

   std::cout << "단어의 수 - " << words.size() << std::endl;
   save("메인.cpp");

   // 벡터      정렬시간 3870 us
   // 리스트   정렬시간 5476 us
}

==========================================================================================
저장한 시간 - 2026-05-11 13:50:39.4179473
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include <chrono>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::cout << "데이터 읽는 중";
   std::vector<ZString> words{ std::istream_iterator<ZString>{in}, {} };
   std::cout << std::endl;

   std::cout << "정렬하는 중";
   auto b = std::chrono::high_resolution_clock::now();
   std::sort(words.begin(), words.end(), [](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });
   auto e = std::chrono::high_resolution_clock::now();
   std::cout << "걸린시간 - " << std::chrono::duration_cast<std::chrono::microseconds>(e - b) << std::endl;

   std::cout << std::endl;

   //for (const ZString& zs : words)
   //   std::cout << zs << std::endl;

   std::cout << "단어의 수 - " << words.size() << std::endl;
   save("메인.cpp");

   // 벡터            정렬시간 3870 us
   // 이동없는 벡터   정렬시간 25804us
   // 리스트         정렬시간 5476 us

   // -> 리스트가 정렬하는데 부적절한가? 그건 아니다. 
   // 벡터는 퀵소트 알고리즘 사용
   // 리스트는 퀵소트가 아닌 머지소트 알고리즘 사용 (리스트는 랜덤 액세스가 안되기 때문에 퀵소트 못씀)
}

==========================================================================================
저장한 시간 - 2026-05-11 13:51:47.2434174
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include <chrono>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::list<ZString> words{ std::istream_iterator<ZString>{in}, {} };

   words.sort([](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });

   for (const ZString& zs : words)
      std::cout << zs << std::endl;

   std::cout << "단어의 수 - " << words.size() << std::endl;
   save("메인.cpp");

   // 벡터            정렬시간 3870 us
   // 이동없는 벡터   정렬시간 25804us
   // 리스트         정렬시간 5476 us

   // -> 리스트가 정렬하는데 부적절한가? 그건 아니다. 
   // 벡터는 퀵소트 알고리즘 사용
   // 리스트는 퀵소트가 아닌 머지소트 알고리즘 사용 (리스트는 랜덤 액세스가 안되기 때문에 퀵소트 못씀)
}

==========================================================================================
저장한 시간 - 2026-05-11 13:58:05.7715301
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include <chrono>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::list<ZString> words{ std::istream_iterator<ZString>{in}, {} };

   words.sort([](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });

   // 정렬한 이후라면 중복단어 제거
   // default operator== 으로 판정
   words.unique();
   // 1. 아무것도 안쓰면 == 연산자로 판별
   // 2. 내가 직접 판별하는 함수(람다)를 만들어서 쓰는 방법


   for (const ZString& zs : words)
      std::cout << zs << std::endl;

   std::cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << std::endl;
   save("메인.cpp");

   // 벡터            정렬시간 3870 us
   // 이동없는 벡터   정렬시간 25804us
   // 리스트         정렬시간 5476 us

   // -> 리스트가 정렬하는데 부적절한가? 그건 아니다. 
   // 벡터는 퀵소트 알고리즘 사용
   // 리스트는 퀵소트가 아닌 머지소트 알고리즘 사용 (리스트는 랜덤 액세스가 안되기 때문에 퀵소트 못씀)
}

==========================================================================================
저장한 시간 - 2026-05-11 14:03:18.3266772
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include <chrono>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   save("메인.cpp");

   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::list<ZString> words{ std::istream_iterator<ZString>{in}, {} };

   words.sort([](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });

   // 정렬한 이후라면 중복단어 제거
   // default operator== 으로 판정
   words.unique();

   for (const ZString& zs : words)
      std::cout << zs << std::endl;

   std::cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << std::endl;

   // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아라.
   // 없으면 없다고 출력하라.
   // 있다면 리스트의 몇번째 단어인지 출력하라.

   while (true) {
      std::cout << "찾을 단어? ";
      ZString word;
      std::cin >> word;

      auto it = std::find(words.begin(), words.end(), word);
      if (it == words.end())
         std::cout << "찾는 단어가 없습니다" << std::endl;
      else
         std::cout << "찾는 단어가 있습니다. 리스트의 " << std::distance(words.begin(), it) + 1 << "번째 단어입니다" << std::endl;
   }
}

==========================================================================================
저장한 시간 - 2026-05-11 14:10:55.1796131
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include <chrono>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   save("메인.cpp");

   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::list<ZString> words{ std::istream_iterator<ZString>{in}, {} };

   words.sort([](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });

   // 정렬한 이후라면 중복단어 제거
   // default operator== 으로 판정
   words.unique();

   for (const ZString& zs : words
      | std::views::take(5)
      | std::views::drop(words.size() - 10)
      | std::views::take(5))
      std::cout << zs << std::endl;

   std::cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << std::endl;

   // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아라.
   // 없으면 없다고 출력하라.
   // 있다면 리스트의 몇번째 단어인지 출력하라.

   while (true) {
      std::cout << "찾을 단어? ";
      ZString word;
      std::cin >> word;

      std::list<ZString>::iterator pos = std::find(words.begin(), words.end(), word);
      
      if (pos == words.end()) {
         std::cout << word << " - 없다" << std::endl;
      }
      else {
         std::cout << std::distance(pos, words.begin()) + 1 << "번째 단어입니다" << std::endl;
      }   
   }
}

==========================================================================================
저장한 시간 - 2026-05-11 14:12:14.0667419
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include <chrono>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   save("메인.cpp");

   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::list<ZString> words{ std::istream_iterator<ZString>{in}, {} };

   words.sort([](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });

   // 정렬한 이후라면 중복단어 제거
   // default operator== 으로 판정
   words.unique();

   for (const ZString& zs : words
      | std::views::take(5))
      std::cout << zs << std::endl;

   std::cout << "-------------------------------" << std::endl;

   for (const ZString& zs : words
      | std::views::reverse
      | std::views::take(5))
      std::cout << zs << std::endl;

   std::cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << std::endl;

   // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아라.
   // 없으면 없다고 출력하라.
   // 있다면 리스트의 몇번째 단어인지 출력하라.

   while (true) {
      std::cout << "찾을 단어? ";
      ZString word;
      std::cin >> word;

      std::list<ZString>::iterator pos = std::find(words.begin(), words.end(), word);
      
      if (pos == words.end()) {
         std::cout << word << " - 없다" << std::endl;
      }
      else {
         std::cout << std::distance(pos, words.begin()) + 1 << "번째 단어입니다" << std::endl;
      }   
   }
}

==========================================================================================
저장한 시간 - 2026-05-11 14:16:23.1654268
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include <chrono>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   save("메인.cpp");

   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::vector<ZString> words{ std::istream_iterator<ZString>{in}, {} };

   std::sort(words.begin(), words.end(), [](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });

   // 정렬한 이후라면 중복단어 제거
   // default operator== 으로 판정
   auto newEnd = std::unique(words.begin(), words.end());
   words.erase(newEnd, words.end());

   for (const ZString& zs : words
      | std::views::take(5))
      std::cout << zs << std::endl;

   std::cout << "-------------------------------" << std::endl;

   for (const ZString& zs : words
      | std::views::reverse
      | std::views::take(5))
      std::cout << zs << std::endl;

   std::cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << std::endl;

   // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아라.
   // 없으면 없다고 출력하라.
   // 있다면 리스트의 몇번째 단어인지 출력하라.

   while (true) {
      std::cout << "찾을 단어? ";
      ZString word;
      std::cin >> word;

      auto pos = std::find(words.begin(), words.end(), word);
      
      if (pos == words.end()) {
         std::cout << word << " - 없다" << std::endl;
      }
      else {
         std::cout << std::distance(pos, words.begin()) + 1 << "번째 단어입니다" << std::endl;
      }   
   }
}

==========================================================================================
저장한 시간 - 2026-05-11 14:29:20.9203579
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include <chrono>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   save("메인.cpp");

   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::vector<ZString> words{ std::istream_iterator<ZString>{in}, {} };

   std::sort(words.begin(), words.end(), [](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });

   // 정렬한 이후라면 중복단어 제거
   // default operator== 으로 판정
   auto newEnd = std::unique(words.begin(), words.end());
   words.erase(newEnd, words.end());

   for (const ZString& zs : words
      | std::views::take(5))
      std::cout << zs << std::endl;

   std::cout << "-------------------------------" << std::endl;

   for (const ZString& zs : words
      | std::views::reverse
      | std::views::take(5))
      std::cout << zs << std::endl;

   std::cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << std::endl;

   // [문제] 사용자가 입력한 단어가 리스트에 있는지 찾아라.
   // 없으면 없다고 출력하라.
   // 있다면 리스트의 몇번째 단어인지 출력하라.

   while (true) {
      std::cout << "찾을 단어? ";
      ZString word;
      std::cin >> word;

      auto pos = std::find(words.begin(), words.end(), word);
      
      if (pos == words.end()) {
         std::cout << word << " - 없다" << std::endl;
      }
      else {
         std::cout << std::distance(pos, words.begin()) + 1 << "번째 단어입니다" << std::endl;
      }   
   }


   // distance (begin, end);
   // vector
   // array
   // T[]
   // std::string
   // ZString
   // -> 메모리 구조가 똑같음. contiguous memory (연속된 메모리) -> 랜덤 억세스 가능 (반드시 contiguous 해야함)
   // 오른쪽으로 갈 수록 메모리 값이 increase. 증가함. 
   // distance는 end에서 begin을 뺀 값을 리턴하는데
   // 우리는 pos, words.begin() 이렇게 넣었으니까 begin()에서 pos를 뺀 값을 리턴함.
   // 근데 list는 어떻게 하냐?
   // 리스트는 이중연결리스트이기 때문에 distance 함수 쓰면 distance(begin, end) 
   // begin에서 end까지 무조건 앞으로 이동하면서 찾음. 그래서 5번째 단어에서 1번째 단어로 가려면 앞으로 3000걸음정도 간 후
   // 이중연결리스트이기 때문에 다시 헤드로 올 수 있음. 그러면 찾게 됨. 
   // 그러면 이중연결리스트이니까 뒤로가게 설계하면 되지 않나? 그게 말처럼 쉽지 않음. 그렇게 복잡하게 함수 설계하지 않음.

   // distance 함수는 인자로 들어오는 컨테이너가 랜덤 억세스 가능한지 아닌지에 따라 다른 알고리즘을 사용함.
   // 구현이 쉽지 않음.

}

==========================================================================================
저장한 시간 - 2026-05-11 14:44:41.8933856
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
#include <list>
#include <fstream>
#include <ranges>
#include <chrono>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   save("메인.cpp");

   // [문제] 파일에 있는 단어를 list<ZString>에 저장하라.
   // 단어를 사전식 오름차순으로 정렬하라.

   std::ifstream in{ "2026년 1학기 STL 월56 화78.txt" };
   if (not in) {
      std::cout << "강의저장 파일이 없네요" << std::endl;
      return 20260504;
   }

   std::vector<ZString> words{ std::istream_iterator<ZString>{in}, {} };

   std::sort(words.begin(), words.end(), [](const ZString& a, const ZString& b) -> bool {
      return std::lexicographical_compare( a.data(), a.data() + a.size(), b.data(), b.data() + b.size());
      });

   // 정렬한 이후라면 중복단어 제거
   // default operator== 으로 판정
   auto newEnd = std::unique(words.begin(), words.end());
   words.erase(newEnd, words.end());


   std::cout << "중복을 제거하고 남은 단어의 수 - " << words.size() << std::endl;

   // [문제] 길이가 10인 단어만 화면에 출력하라. (STL 배운 사람이라면 아마 이렇게 코딩할 것)
   copy_if(words.begin(), words.end(), std::ostream_iterator<ZString>(std::cout, "\n"), [](const ZString& zs) {
      return 10 == zs.size();
      });

}

==========================================================================================
저장한 시간 - 2026-05-11 14:58:40.4030874
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
// - deque<T> - vector와 list의 장점을 갖는 컨테이너
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <deque>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   save("메인.cpp");

   // vector / list / deque 중 원소를 하나씩 push_back (혹은 emplace_back)할 때 원소를 가장 많이 못담는 컨테이너는 벡터.
   // 그럼 가장 많이 담는 컨테이너는 덱. MSVC에서 dequq은 16바이트 당 포인터를 달아서 관리한다.
   // GCC나 clang같은 곳에서는 512바이트, 4096바이트 등 다 다르다.
   // deque > list > vector 순으로 데이터를 많이 담을 수 있다.

   std::deque<int> d{ 1, 2, 3, 4 };
   // contiguous 메모리 블록에 들어간다는 소리. (int x 4 = 16바이트)
   
   for (int& num : d)
      std::cout << std::addressof(num) << std::endl;
}

==========================================================================================
저장한 시간 - 2026-05-11 15:00:42.0192621
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
// - deque<T> - vector와 list의 장점을 갖는 컨테이너
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <deque>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   save("메인.cpp");

   std::deque<int> d{ 1, 2, 3, 4 };
   
   for (int& num : d)
      std::cout << std::addressof(num) << std::endl;

   d.push_front(0);
   d.push_back(5);
   // 이러는 순간 덱이 세개로 늘어나며 효율 낮아짐.
   // 0 / 1, 2, 3, 4 / 5 -> 이렇게

   for (int num: d)
      std::cout << num << ' ';
   std::cout << std::endl;

   std::cout << std::endl;
   for (int& num: d)
      std::cout << std::addressof(num) << std::endl;
}

==========================================================================================
저장한 시간 - 2026-05-11 15:02:45.3679652
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
// - deque<T> - vector와 list의 장점을 갖는 컨테이너
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <deque>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   save("메인.cpp");

   관찰 = true;
   std::deque<ZString> d{ "1", "22", "333", "4444" };
   
   for (const ZString& zs : d)
      zs.show();

   관찰 = false;
}

==========================================================================================
저장한 시간 - 2026-05-11 15:05:51.1595857
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
// - deque<T> - vector와 list의 장점을 갖는 컨테이너
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <list>
#include <deque>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   save("메인.cpp");

   관찰 = true;
   std::deque<ZString> d{ "1", "22", "333", "4444" };
   
   for (int i = 0; i < d.size(); ++i)
      d[i];      // 근데 contiguous한 메모리처럼 접근 가능? 말이 되나 이게

   // 객체마다 주소가 따로따로. 엉망임. ZString은 24바이트 이기 때문. -> list처럼 동작.
   관찰 = false;
}

==========================================================================================
저장한 시간 - 2026-05-11 15:09:52.4549187
==========================================================================================

//-------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56 화78      05월 11일                                       (9주 2일)
//------------------------------------------------------------------------------------------------------- 
// STL 컨테이너 - Containers are objects that store ohter objects.
// Sequence Container
// - array<T, N> - 유일하게 컴파일 타임에 size 결정 - STACK, DATA
// - vector<T> - 캐시 히트율이 높아 고속 데이터 처리에 유리
// - list<T> - 아무데서나 원소 추가/삭제 O(1)
// - deque<T> - vector와 list의 장점을 갖는 컨테이너
//-------------------------------------------------------------------------------------------------------
#include <iostream>
#include <vector>
#include <list>
#include <deque>
#include "save.h"
#include "ZString.h"

extern bool 관찰;            // 관찰하려면 true

// ----------
int main()
// ----------
{
   save("메인.cpp");

   std::vector<int> v;

   size_t cnt{};
   while (true) {
      try {
         v.push_back(1);
      } 
      catch (...) {      // ... 은 타원들(ellipses)라고 불리는 문법 용어. 모든 예외를 잡아내는 catch 블록.
         std::cout << std::endl;
         std::cout << "벡터 - " << v.size() << std::endl;
         break;
      }

      if (not (++cnt % 100'0000))
         std::cout << ".";
   }

   관찰 = false;
}