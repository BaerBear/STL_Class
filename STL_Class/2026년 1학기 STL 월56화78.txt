//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 1일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string& fname); // string은 좀 무거움. string_view는 나중에 다룰 것. 

int main() 
{
	std::cout << "2026년 3월 3일" << '\n';

	save("메인.cpp"); // 메인.cpp를 저장하는 함수를 만들거다.
					 // 빨간줄 난 save의 식별자?? -> save라는 이름의 함수가 없어서. 다른 함수와 식별이 되어야하니까. 
					 // 선언(declare) 해줘야함. 얼마든지 해도 상관없음.

//	return 0; -> 이건 C++ 표준이 아님. 이렇게 쓰는 책 있으면 집어 던지셈.
}

// 여기 설명부터 월요일에 다시
// fname부터

void save(const std::string& fname)
{
	std::ifstream in{ fname };
	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app};
	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}
//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 1일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string& fname); // string은 좀 무거움. string_view는 나중에 다룰 것. 

int main() 
{
	std::cout << "2026년 3월 3일" << '\n';

	save("메인.cpp"); // 메인.cpp를 저장하는 함수를 만들거다.
					 // 빨간줄 난 save의 식별자?? -> save라는 이름의 함수가 없어서. 다른 함수와 식별이 되어야하니까. 
					 // 선언(declare) 해줘야함. 얼마든지 해도 상관없음.

//	return 0; -> 이건 C++ 표준이 아님. 이렇게 쓰는 책 있으면 집어 던지셈.
}

// 여기 설명부터 월요일에 다시
// fname부터

void save(const std::string& fname)
{
	std::ifstream in{ fname };
	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app};
	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}
//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 1일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string& fname); // string은 좀 무거움. string_view는 나중에 다룰 것. 

int main() 
{
	std::cout << "2026년 3월 3일" << '\n';

	save("메인.cpp"); // 메인.cpp를 저장하는 함수를 만들거다.
					 // 빨간줄 난 save의 식별자?? -> save라는 이름의 함수가 없어서. 다른 함수와 식별이 되어야하니까. 
					 // 선언(declare) 해줘야함. 얼마든지 해도 상관없음.

//	return 0; -> 이건 C++ 표준이 아님. 이렇게 쓰는 책 있으면 집어 던지셈.
}

// 여기 설명부터 월요일에 다시
// fname부터

void save(const std::string& fname)
{
	std::ifstream in{ fname };
	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app};
	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}
//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 1일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string& fname); // string은 좀 무거움. string_view는 나중에 다룰 것. 

// 사용하지 말아야 할 자료형 -> 대체할 수 있다 -> zero-overhead (대체한다고 해서 불이익이 없다는 뜻)
// 1. char* -> std::string
// char* -> 문자열을 처리하기 위한 특별한 자료형. 왜 쓰지말아야 하냐
// 이런 자료형이 할 수 있는게 아무것도 없음
// 2. T*	-> snart pointer 를 사용할 것
// 3. T[]	-> std::array 를 사용할 것
// ex) int num [100];
// num[100]에 접근하려다가 범위가 넘어가버리기 십상이다. (버퍼 오버플로우)
//

int main() 
{
	std::cout << "2026년 3월 9일" << '\n';

	save("메인.cpp"); // 메인.cpp를 저장하는 함수를 만들거다.
					 // 빨간줄 난 save의 식별자?? -> save라는 이름의 함수가 없어서. 다른 함수와 식별이 되어야하니까. 
					 // 선언(declare) 해줘야함. 얼마든지 해도 상관없음.

//	return 0; -> 이건 C++ 표준이 아님. 이렇게 쓰는 책 있으면 집어 던지셈.
}

// 여기 설명부터 월요일에 다시
// fname부터
// naming convention - 이름 짓는 규칙
// fileName - snake style
// file_name - nata style

void save(const std::string& fname)
{
	std::ifstream in{ fname }; // #include <fstream>. RAII -> fname의 생성과	소멸이 자동으로 관리된다.
	// in은 새로운 인스턴스. in은 ifstream 클래스의 객체. in은 파일을 읽어오는 객체. fname은 파일 이름.
	// in.close -> 쓰면 바보. in이 소멸될 때 자동으로 close가 된다.

	if (not in) { // ! 쓰면 읽기 힘드니까 not으로 바꾸는 습관 들이기
		std::cout << fname << " - 열 수 없습니다." << '\n';
		// return; -> 이것은 함수를 호출한 곳으로 돌아감.
		exit(20260309);
	}

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app};
	// out은 파일을 쓰는 새로운 인스턴스. 두번째 인자에 따라 파일이 새로 만들어질 수도 있고, 기존 파일에 이어서 쓸 수도 있다.
	// std::ios::app -> append mode. 기존 파일이 있으면 그 뒤에 이어서 쓴다. 기존 파일이 없으면 새로 만든다.

	char c;
	in >> std::noskipws; // 선생님 공백도 읽어주세요
	while (in >> c) { // in에서 한 글자씩 읽어서 c에 저장. 읽을 수 없으면 false가 된다.
		out << c; // out에 c를 쓴다.
	}

	/*std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});*/
}
//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string& fname); // string은 좀 무거움. string_view는 나중에 다룰 것. 

// 사용하지 말아야 할 자료형 -> 대체할 수 있다 -> zero-overhead (대체한다고 해서 불이익이 없다는 뜻)
// 1. char* -> std::string
// char* -> 문자열을 처리하기 위한 특별한 자료형. 왜 쓰지말아야 하냐
// 이런 자료형이 할 수 있는게 아무것도 없음
// 2. T*	-> snart pointer 를 사용할 것
// 3. T[]	-> std::array 를 사용할 것
// ex) int num [100];
// num[100]에 접근하려다가 범위가 넘어가버리기 십상이다. (버퍼 오버플로우)
//

int main() 
{
	std::cout << "2026년 3월 9일" << '\n';

	save("메인.cpp"); // 메인.cpp를 저장하는 함수를 만들거다.
					 // 빨간줄 난 save의 식별자?? -> save라는 이름의 함수가 없어서. 다른 함수와 식별이 되어야하니까. 
					 // 선언(declare) 해줘야함. 얼마든지 해도 상관없음.

//	return 0; -> 이건 C++ 표준이 아님. 이렇게 쓰는 책 있으면 집어 던지셈.
}

// 여기 설명부터 월요일에 다시
// fname부터
// naming convention - 이름 짓는 규칙
// fileName - snake style
// file_name - nata style

void save(const std::string& fname)
{
	std::ifstream in{ fname }; // #include <fstream>. RAII -> fname의 생성과	소멸이 자동으로 관리된다.
	// in은 새로운 인스턴스. in은 ifstream 클래스의 객체. in은 파일을 읽어오는 객체. fname은 파일 이름.
	// in.close -> 쓰면 바보. in이 소멸될 때 자동으로 close가 된다.

	if (not in) { // ! 쓰면 읽기 힘드니까 not으로 바꾸는 습관 들이기
		std::cout << fname << " - 열 수 없습니다." << '\n';
		// return; -> 이것은 함수를 호출한 곳으로 돌아감.
		exit(20260309);
	}

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app};
	// out은 파일을 쓰는 새로운 인스턴스. 두번째 인자에 따라 파일이 새로 만들어질 수도 있고, 기존 파일에 이어서 쓸 수도 있다.
	// std::ios::app -> append mode. 기존 파일이 있으면 그 뒤에 이어서 쓴다. 기존 파일이 없으면 새로 만든다.

	//char c;
	//in >> std::noskipws; // 선생님 공백도 읽어주세요
	//while (in >> c) {	 // in에서 한 글자씩 읽어서 c에 저장. 읽을 수 없으면 false가 된다.
	//	out << c;		 // out에 c를 쓴다.
	//} -> copy 알고리즘

	// std::copy(어디에서, 어디까지, 어디로);
	// iterator -> STL의 핵심
	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}
//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string& fname); // string은 좀 무거움. string_view는 나중에 다룰 것. 

// 사용하지 말아야 할 자료형 -> 대체할 수 있다 -> zero-overhead (대체한다고 해서 불이익이 없다는 뜻)
// 1. char* -> std::string
// char* -> 문자열을 처리하기 위한 특별한 자료형. 왜 쓰지말아야 하냐
// 이런 자료형이 할 수 있는게 아무것도 없음
// 2. T*	-> snart pointer 를 사용할 것
// 3. T[]	-> std::array 를 사용할 것
// ex) int num [100];
// num[100]에 접근하려다가 범위가 넘어가버리기 십상이다. (버퍼 오버플로우)
//

int main() 
{
	std::cout << "2026년 3월 9일" << '\n';

	save("메인.cpp"); // 메인.cpp를 저장하는 함수를 만들거다.
					 // 빨간줄 난 save의 식별자?? -> save라는 이름의 함수가 없어서. 다른 함수와 식별이 되어야하니까. 
					 // 선언(declare) 해줘야함. 얼마든지 해도 상관없음.

//	return 0; -> 이건 C++ 표준이 아님. 이렇게 쓰는 책 있으면 집어 던지셈.
}

// 여기 설명부터 월요일에 다시
// fname부터
// naming convention - 이름 짓는 규칙
// fileName - snake style
// file_name - nata style

void save(const std::string& fname)
{
	std::ifstream in{ fname }; // #include <fstream>. RAII -> fname의 생성과	소멸이 자동으로 관리된다.
	// in은 새로운 인스턴스. in은 ifstream 클래스의 객체. in은 파일을 읽어오는 객체. fname은 파일 이름.
	// in.close -> 쓰면 바보. in이 소멸될 때 자동으로 close가 된다.

	if (not in) { // ! 쓰면 읽기 힘드니까 not으로 바꾸는 습관 들이기
		std::cout << fname << " - 열 수 없습니다." << '\n';
		// return; -> 이것은 함수를 호출한 곳으로 돌아감.
		exit(20260309);
	}

	//--------------------------------------------------------------------------------------------------------------------
	// 저장한 시간
	//--------------------------------------------------------------------------------------------------------------------
	std::cout << "\"" << fname << "\"를 저장합니다." << "\n";


	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app};
	// out은 파일을 쓰는 새로운 인스턴스. 두번째 인자에 따라 파일이 새로 만들어질 수도 있고, 기존 파일에 이어서 쓸 수도 있다.
	// std::ios::app -> append mode. 기존 파일이 있으면 그 뒤에 이어서 쓴다. 기존 파일이 없으면 새로 만든다.

	// std::copy(어디에서, 어디까지, 어디로);
	// iterator -> STL의 핵심
	// std::istream_iterator<char>{in}	  -> in에서 char를 읽어오는 iterator. in이 끝날 때까지 읽는다.
	// std::istreambuf_iterator<char>{in} -> in에서 char를 읽어오는 iterator. 버퍼 단위로 읽는다. 더 빠르다.
	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}


==================================================
저장한 시간 - 
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string& fname); // string은 좀 무거움. string_view는 나중에 다룰 것. 

// 사용하지 말아야 할 자료형 -> 대체할 수 있다 -> zero-overhead (대체한다고 해서 불이익이 없다는 뜻)
// 1. char* -> std::string
// char* -> 문자열을 처리하기 위한 특별한 자료형. 왜 쓰지말아야 하냐
// 이런 자료형이 할 수 있는게 아무것도 없음
// 2. T*	-> snart pointer 를 사용할 것
// 3. T[]	-> std::array 를 사용할 것
// ex) int num [100];
// num[100]에 접근하려다가 범위가 넘어가버리기 십상이다. (버퍼 오버플로우)
//

int main() 
{
	std::cout << "2026년 3월 9일" << '\n';

	save("메인.cpp"); // 메인.cpp를 저장하는 함수를 만들거다.
					 // 빨간줄 난 save의 식별자?? -> save라는 이름의 함수가 없어서. 다른 함수와 식별이 되어야하니까. 
					 // 선언(declare) 해줘야함. 얼마든지 해도 상관없음.

//	return 0; -> 이건 C++ 표준이 아님. 이렇게 쓰는 책 있으면 집어 던지셈.
}

// 여기 설명부터 월요일에 다시
// fname부터
// naming convention - 이름 짓는 규칙
// fileName - snake style
// file_name - nata style

void save(const std::string& fname)
{
	std::ifstream in{ fname }; // #include <fstream>. RAII -> fname의 생성과	소멸이 자동으로 관리된다.
	// in은 새로운 인스턴스. in은 ifstream 클래스의 객체. in은 파일을 읽어오는 객체. fname은 파일 이름.
	// in.close -> 쓰면 바보. in이 소멸될 때 자동으로 close가 된다.

	if (not in) { // ! 쓰면 읽기 힘드니까 not으로 바꾸는 습관 들이기
		std::cout << fname << " - 열 수 없습니다." << '\n';
		// return; -> 이것은 함수를 호출한 곳으로 돌아감.
		exit(20260309);
	}

	std::cout << "\"" << fname << "\"를 저장합니다." << "\n";

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app};
	
	// 시간정보 얻기
	
	// time epoch(시간의 기원)로 부터 1초마다 증가한 tick 수. 1970년 1월 1일 0시 0분 0초
	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now(); 
	// 데이터 타입 deduction. auto는 컴파일러가 알아서 데이터 타입을 추론하게 하는 키워드. 

	std::cout << now << '\n';

	out << '\n' << '\n';
	out << "==================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "==================================================" << '\n';
	out << '\n';

	// out은 파일을 쓰는 새로운 인스턴스. 두번째 인자에 따라 파일이 새로 만들어질 수도 있고, 기존 파일에 이어서 쓸 수도 있다.
	// std::ios::app -> append mode. 기존 파일이 있으면 그 뒤에 이어서 쓴다. 기존 파일이 없으면 새로 만든다.

	// std::copy(어디에서, 어디까지, 어디로);
	// iterator -> STL의 핵심
	// std::istream_iterator<char>{in}	  -> in에서 char를 읽어오는 iterator. in이 끝날 때까지 읽는다.
	// std::istreambuf_iterator<char>{in} -> in에서 char를 읽어오는 iterator. 버퍼 단위로 읽는다. 더 빠르다.
	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}


==================================================
저장한 시간 - 2026-03-09 14:21:59.4935198
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

// using namespace std; -> 대규모 프로젝트 관리를 위해 쓰지않음

void save(const std::string& fname); // string은 좀 무거움. string_view는 나중에 다룰 것. 

// 사용하지 말아야 할 자료형 -> 대체할 수 있다 -> zero-overhead (대체한다고 해서 불이익이 없다는 뜻)
// 1. char* -> std::string
// char* -> 문자열을 처리하기 위한 특별한 자료형. 왜 쓰지말아야 하냐
// 이런 자료형이 할 수 있는게 아무것도 없음
// 2. T*	-> snart pointer 를 사용할 것
// 3. T[]	-> std::array 를 사용할 것
// ex) int num [100];
// num[100]에 접근하려다가 범위가 넘어가버리기 십상이다. (버퍼 오버플로우)
//

int main() 
{
	std::cout << "2026년 3월 9일" << '\n';

	save("메인.cpp"); // 메인.cpp를 저장하는 함수를 만들거다.
					 // 빨간줄 난 save의 식별자?? -> save라는 이름의 함수가 없어서. 다른 함수와 식별이 되어야하니까. 
					 // 선언(declare) 해줘야함. 얼마든지 해도 상관없음.

//	return 0; -> 이건 C++ 표준이 아님. 이렇게 쓰는 책 있으면 집어 던지셈.
}

// 여기 설명부터 월요일에 다시
// fname부터
// naming convention - 이름 짓는 규칙
// fileName - snake style
// file_name - nata style

void save(const std::string& fname)
{
	std::ifstream in{ fname }; // #include <fstream>. RAII -> fname의 생성과	소멸이 자동으로 관리된다.
	// in은 새로운 인스턴스. in은 ifstream 클래스의 객체. in은 파일을 읽어오는 객체. fname은 파일 이름.
	// in.close -> 쓰면 바보. in이 소멸될 때 자동으로 close가 된다.

	if (not in) { // ! 쓰면 읽기 힘드니까 not으로 바꾸는 습관 들이기
		std::cout << fname << " - 열 수 없습니다." << '\n';
		// return; -> 이것은 함수를 호출한 곳으로 돌아감.
		exit(20260309);
	}

	std::cout << "\"" << fname << "\"를 저장합니다." << "\n";

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app};
	
	// 시간정보 얻기
	
	// time epoch(시간의 기원)로 부터 1초마다 증가한 tick 수. 1970년 1월 1일 0시 0분 0초
	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now(); 
	// 데이터 타입 deduction. auto는 컴파일러가 알아서 데이터 타입을 추론하게 하는 키워드. 

	using namespace std::chrono_literals; // 9h -> 9시간. 9s -> 9초. 9ms -> 9밀리초. 9us -> 9마이크로초. 9ns -> 9나노초.

	out << '\n' << '\n';
	out << "==================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "==================================================" << '\n';
	out << '\n';

	// out은 파일을 쓰는 새로운 인스턴스. 두번째 인자에 따라 파일이 새로 만들어질 수도 있고, 기존 파일에 이어서 쓸 수도 있다.
	// std::ios::app -> append mode. 기존 파일이 있으면 그 뒤에 이어서 쓴다. 기존 파일이 없으면 새로 만든다.

	// std::copy(어디에서, 어디까지, 어디로);
	// iterator -> STL의 핵심
	// std::istream_iterator<char>{in}	  -> in에서 char를 읽어오는 iterator. in이 끝날 때까지 읽는다.
	// std::istreambuf_iterator<char>{in} -> in에서 char를 읽어오는 iterator. 버퍼 단위로 읽는다. 더 빠르다.
	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}


==================================================
저장한 시간 - 2026년 3월 9일 23시 33분 56초
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

// using namespace std; -> 대규모 프로젝트 관리를 위해 쓰지않음

void save(const std::string& fname); // string은 좀 무거움. string_view는 나중에 다룰 것. 

// 사용하지 말아야 할 자료형 -> 대체할 수 있다 -> zero-overhead (대체한다고 해서 불이익이 없다는 뜻)
// 1. char* -> std::string
// char* -> 문자열을 처리하기 위한 특별한 자료형. 왜 쓰지말아야 하냐
// 이런 자료형이 할 수 있는게 아무것도 없음
// 2. T*	-> snart pointer 를 사용할 것
// 3. T[]	-> std::array 를 사용할 것
// ex) int num [100];
// num[100]에 접근하려다가 범위가 넘어가버리기 십상이다. (버퍼 오버플로우)
//

int main() 
{
	std::cout << "2026년 3월 9일" << '\n';

	save("메인.cpp"); // 메인.cpp를 저장하는 함수를 만들거다.
					 // 빨간줄 난 save의 식별자?? -> save라는 이름의 함수가 없어서. 다른 함수와 식별이 되어야하니까. 
					 // 선언(declare) 해줘야함. 얼마든지 해도 상관없음.

//	return 0; -> 이건 C++ 표준이 아님. 이렇게 쓰는 책 있으면 집어 던지셈.
}

// 여기 설명부터 월요일에 다시
// fname부터
// naming convention - 이름 짓는 규칙
// fileName - snake style
// file_name - nata style

void save(const std::string& fname)
{
	std::ifstream in{ fname }; // #include <fstream>. RAII -> fname의 생성과	소멸이 자동으로 관리된다.
	// in은 새로운 인스턴스. in은 ifstream 클래스의 객체. in은 파일을 읽어오는 객체. fname은 파일 이름.
	// in.close -> 쓰면 바보. in이 소멸될 때 자동으로 close가 된다.

	if (not in) { // ! 쓰면 읽기 힘드니까 not으로 바꾸는 습관 들이기
		std::cout << fname << " - 열 수 없습니다." << '\n';
		// return; -> 이것은 함수를 호출한 곳으로 돌아감.
		exit(20260309);
	}

	std::cout << "\"" << fname << "\"를 저장합니다." << "\n";

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app};
	
	// 시간정보 얻기
	
	// time epoch(시간의 기원)로 부터 1초마다 증가한 tick 수. 1970년 1월 1일 0시 0분 0초
	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	using namespace std::chrono_literals; // 9h -> 9시간. 9s -> 9초. 9ms -> 9밀리초. 9us -> 9마이크로초. 9ns -> 9나노초.
	
	auto now = std::chrono::system_clock::now() + 9h; 
	// 데이터 타입 deduction. auto는 컴파일러가 알아서 데이터 타입을 추론하게 하는 키워드. 

	// time_point -> time_t
	std::time_t tt = std::chrono::system_clock::to_time_t(now);

	// time_t -> tm
	std::tm local = *std::localtime(&tt);

	out << '\n' << '\n';
	out << "==================================================" << '\n';
	out << "저장한 시간 - "
		<< (local.tm_year + 1900) << "년 "
		<< (local.tm_mon + 1) << "월 "
		<< local.tm_mday << "일 "
		<< local.tm_hour << "시 "
		<< local.tm_min << "분 "
		<< local.tm_sec << "초" << '\n';
	out << "==================================================" << '\n';
	out << '\n';

	// out은 파일을 쓰는 새로운 인스턴스. 두번째 인자에 따라 파일이 새로 만들어질 수도 있고, 기존 파일에 이어서 쓸 수도 있다.
	// std::ios::app -> append mode. 기존 파일이 있으면 그 뒤에 이어서 쓴다. 기존 파일이 없으면 새로 만든다.

	// std::copy(어디에서, 어디까지, 어디로);
	// iterator -> STL의 핵심
	// std::istream_iterator<char>{in}	  -> in에서 char를 읽어오는 iterator. in이 끝날 때까지 읽는다.
	// std::istreambuf_iterator<char>{in} -> in에서 char를 읽어오는 iterator. 버퍼 단위로 읽는다. 더 빠르다.
	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}


==================================================
저장한 시간 - 2026년 3월 9일 14시 34분 20초
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

// using namespace std; -> 대규모 프로젝트 관리를 위해 쓰지않음

void save(const std::string& fname); // string은 좀 무거움. string_view는 나중에 다룰 것. 

// 사용하지 말아야 할 자료형 -> 대체할 수 있다 -> zero-overhead (대체한다고 해서 불이익이 없다는 뜻)
// 1. char* -> std::string
// char* -> 문자열을 처리하기 위한 특별한 자료형. 왜 쓰지말아야 하냐
// 이런 자료형이 할 수 있는게 아무것도 없음
// 2. T*	-> snart pointer 를 사용할 것
// 3. T[]	-> std::array 를 사용할 것
// ex) int num [100];
// num[100]에 접근하려다가 범위가 넘어가버리기 십상이다. (버퍼 오버플로우)
//

int main() 
{
	std::cout << "2026년 3월 9일" << '\n';

	save("메인.cpp"); // 메인.cpp를 저장하는 함수를 만들거다.
					 // 빨간줄 난 save의 식별자?? -> save라는 이름의 함수가 없어서. 다른 함수와 식별이 되어야하니까. 
					 // 선언(declare) 해줘야함. 얼마든지 해도 상관없음.

//	return 0; -> 이건 C++ 표준이 아님. 이렇게 쓰는 책 있으면 집어 던지셈.
}

// 여기 설명부터 월요일에 다시
// fname부터
// naming convention - 이름 짓는 규칙
// fileName - snake style
// file_name - nata style

void save(const std::string& fname)
{
	std::ifstream in{ fname }; // #include <fstream>. RAII -> fname의 생성과	소멸이 자동으로 관리된다.
	// in은 새로운 인스턴스. in은 ifstream 클래스의 객체. in은 파일을 읽어오는 객체. fname은 파일 이름.
	// in.close -> 쓰면 바보. in이 소멸될 때 자동으로 close가 된다.

	if (not in) { // ! 쓰면 읽기 힘드니까 not으로 바꾸는 습관 들이기
		std::cout << fname << " - 열 수 없습니다." << '\n';
		// return; -> 이것은 함수를 호출한 곳으로 돌아감.
		exit(20260309);
	}

	std::cout << "\"" << fname << "\"를 저장합니다." << "\n";

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app};
	
	// 시간정보 얻기
	
	// time epoch(시간의 기원)로 부터 1초마다 증가한 tick 수. 1970년 1월 1일 0시 0분 0초
	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	using namespace std::chrono_literals; // 9h -> 9시간. 9s -> 9초. 9ms -> 9밀리초. 9us -> 9마이크로초. 9ns -> 9나노초.
	
	auto now = std::chrono::system_clock::now(); 
	// 데이터 타입 deduction. auto는 컴파일러가 알아서 데이터 타입을 추론하게 하는 키워드. 

	// time_point -> time_t
	std::time_t tt = std::chrono::system_clock::to_time_t(now);

	// time_t -> tm
	std::tm local = *std::localtime(&tt);

	out << '\n' << '\n';
	out << "==================================================" << '\n';
	out << "저장한 시간 - "
		<< (local.tm_year + 1900) << "년 "
		<< (local.tm_mon + 1) << "월 "
		<< local.tm_mday << "일 "
		<< local.tm_hour << "시 "
		<< local.tm_min << "분 "
		<< local.tm_sec << "초" << '\n';
	out << "==================================================" << '\n';
	out << '\n';

	// out은 파일을 쓰는 새로운 인스턴스. 두번째 인자에 따라 파일이 새로 만들어질 수도 있고, 기존 파일에 이어서 쓸 수도 있다.
	// std::ios::app -> append mode. 기존 파일이 있으면 그 뒤에 이어서 쓴다. 기존 파일이 없으면 새로 만든다.

	// std::copy(어디에서, 어디까지, 어디로);
	// iterator -> STL의 핵심
	// std::istream_iterator<char>{in}	  -> in에서 char를 읽어오는 iterator. in이 끝날 때까지 읽는다.
	// std::istreambuf_iterator<char>{in} -> in에서 char를 읽어오는 iterator. 버퍼 단위로 읽는다. 더 빠르다.
	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}


==================================================
저장한 시간 - 2026년 03월 09일 14시 38분 27.4319048초
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 한 학기 강의를 자동 저장하는 save 함수를 만든다.
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

// using namespace std; -> 대규모 프로젝트 관리를 위해 쓰지않음

void save(const std::string& fname); // string은 좀 무거움. string_view는 나중에 다룰 것. 

// 사용하지 말아야 할 자료형 -> 대체할 수 있다 -> zero-overhead (대체한다고 해서 불이익이 없다는 뜻)
// 1. char* -> std::string
// char* -> 문자열을 처리하기 위한 특별한 자료형. 왜 쓰지말아야 하냐
// 이런 자료형이 할 수 있는게 아무것도 없음
// 2. T*	-> snart pointer 를 사용할 것
// 3. T[]	-> std::array 를 사용할 것
// ex) int num [100];
// num[100]에 접근하려다가 범위가 넘어가버리기 십상이다. (버퍼 오버플로우)
//

int main() 
{
	std::cout << "2026년 3월 9일" << '\n';

	save("메인.cpp"); // 메인.cpp를 저장하는 함수를 만들거다.
					 // 빨간줄 난 save의 식별자?? -> save라는 이름의 함수가 없어서. 다른 함수와 식별이 되어야하니까. 
					 // 선언(declare) 해줘야함. 얼마든지 해도 상관없음.

//	return 0; -> 이건 C++ 표준이 아님. 이렇게 쓰는 책 있으면 집어 던지셈.
}

// 여기 설명부터 월요일에 다시
// fname부터
// naming convention - 이름 짓는 규칙
// fileName - snake style
// file_name - nata style

void save(const std::string& fname)
{
	std::ifstream in{ fname }; // #include <fstream>. RAII -> fname의 생성과	소멸이 자동으로 관리된다.
	// in은 새로운 인스턴스. in은 ifstream 클래스의 객체. in은 파일을 읽어오는 객체. fname은 파일 이름.
	// in.close -> 쓰면 바보. in이 소멸될 때 자동으로 close가 된다.

	if (not in) { // ! 쓰면 읽기 힘드니까 not으로 바꾸는 습관 들이기
		std::cout << fname << " - 열 수 없습니다." << '\n';
		// return; -> 이것은 함수를 호출한 곳으로 돌아감.
		exit(20260309);
	}

	std::cout << "\"" << fname << "\"를 저장합니다." << "\n";

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app};
	
	// 시간정보 얻기
	using namespace std::chrono_literals; // 9h -> 9시간. 9s -> 9초. 9ms -> 9밀리초. 9us -> 9마이크로초. 9ns -> 9나노초.
	
	// time epoch(시간의 기원)로 부터 1초마다 증가한 tick 수. 1970년 1월 1일 0시 0분 0초
	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now() + 9h; 
	// 데이터 타입 deduction. auto는 컴파일러가 알아서 데이터 타입을 추론하게 하는 키워드. 

	out << '\n' << '\n';
	out << "==================================================" << '\n';
	out << "저장한 시간 - "
		<< std::format("{:%Y년 %m월 %d일 %H시 %M분 %S초}", now) << '\n';
	out << "==================================================" << '\n';
	out << '\n';

	// out은 파일을 쓰는 새로운 인스턴스. 두번째 인자에 따라 파일이 새로 만들어질 수도 있고, 기존 파일에 이어서 쓸 수도 있다.
	// std::ios::app -> append mode. 기존 파일이 있으면 그 뒤에 이어서 쓴다. 기존 파일이 없으면 새로 만든다.

	// std::copy(어디에서, 어디까지, 어디로);
	// iterator -> STL의 핵심
	// std::istream_iterator<char>{in}	  -> in에서 char를 읽어오는 iterator. in이 끝날 때까지 읽는다.
	// std::istreambuf_iterator<char>{in} -> in에서 char를 읽어오는 iterator. 버퍼 단위로 읽는다. 더 빠르다.
	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostream_iterator<char>{out});
}


==================================================
저장한 시간 - 2026년 03월 09일 14시 53분 04.4562061초
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// save 파일 분리
// template
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

int main() 
{
	std::cout << "save를 파일로 분리하였다" << "\n";

	save("메인.cpp"); 
}


==================================================
저장한 시간 - 2026년 03월 09일 14시 55분 30.9668441초
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// save 파일 분리
// template
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"
// using namespace std; -> 강의 시 가독성을 위해서 사용함. 나는 이렇게 하지 말자. 시험 땐 명시 돼있음. std빼고 써도 됨

//--------
int main() 
//--------
{
	save("메인.cpp"); 
}


==================================================
저장한 시간 - 2026년 03월 09일 14시 58분 19.3378094초
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// save 파일 분리
// template
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void change(int n1, int n2);

//--------
int main() 
//--------
{
	// [문제] 의도대로 실행되게 하자. (시험문제)


	int a{ 1 }, b{ 2 };
	
	// 여기서 change를 호출하였다.
	change(a, b);

	std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함
	save("메인.cpp"); 
}

void change(int n1, int n2) {
	std::swap(n1, n2);
}

==================================================
저장한 시간 - 2026년 03월 09일 15시 00분 02.7474356초
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// save 파일 분리
// template
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void change(int&, int&);

//--------
int main() 
//--------
{
	// [문제] 의도대로 실행되게 하자. (시험문제)


	int a{ 1 }, b{ 2 };
	
	// 여기서 change를 호출하였다.
	change(a, b);

	std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함
	save("메인.cpp"); 
}

void change(int& n1, int& n2) {
	std::swap(n1, n2);
}

==================================================
저장한 시간 - 2026년 03월 09일 15시 07분 13.7839773초
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// save 파일 분리
// template
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void change(int&, int&);

//--------
int main() 
//--------
{
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;

	// [문제] 의도대로 실행되게 하자. (시험문제)
	int a{ 1 }, b{ 2 };
	std::cout << "a 주소 - " << std::addressof(a) << std::endl;


	// 여기서 change를 호출하였다. 
	// 함수라고도 할 수 있지만 함수 이외에 호출할 수 있는 타입이 많다. callable 타입의 갯수는 무한개 (자료형이 다르기 때문)
	change(a, b);

	std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함
	save("메인.cpp"); 
}

void change(int& n1, int& n2) {
	std::swap(n1, n2);
}

==================================================
저장한 시간 - 2026년 03월 09일 15시 08분 18.0116153초
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// save 파일 분리
// template
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void change(int&, int&);
int a{ 1 }, b{ 2 };

//--------
int main() 
//--------
{
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;

	// [문제] 의도대로 실행되게 하자. (시험문제)
	std::cout << "a 주소 - " << std::addressof(a) << std::endl;


	// 여기서 change를 호출하였다. 
	// 함수라고도 할 수 있지만 함수 이외에 호출할 수 있는 타입이 많다. callable 타입의 갯수는 무한개 (자료형이 다르기 때문)
	change(a, b);

	std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함
	save("메인.cpp"); 
}

void change(int& n1, int& n2) {
	std::swap(n1, n2);
}

==================================================
저장한 시간 - 2026년 03월 09일 15시 16분 42.7213239초
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일			(1주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// save 파일 분리
// template
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include "save.h"

void change(int&, int&);

//--------
int main() 
//--------
{
	// [문제] 의도대로 실행되게 하자. (시험문제)
	int a{ 1 }, b{ 2 };

	// 여기서 change를 호출하였다. 
	// 함수라고도 할 수 있지만 함수 이외에 호출할 수 있는 타입이 많다. callable 타입의 갯수는 무한개 (자료형이 다르기 때문)
	change(a, b);

	std::cout << a << ", " << b << std::endl;		// 2, 1이라고 출력되어야 함
	save("메인.cpp"); 
}

void change(int& n1, int& n2) {
	// & -> 레퍼런스는 컴파일러가 자동으로 포인터로 바꿔주는 문법적 편의장치.
	// 레퍼런스는 포인터의 예쁜 가면일 뿐이다.
	std::swap(n1, n2);
}

==================================================
저장한 시간 - 2026년 03월 16일 14시 59분 11.0860666초
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 10일			(2주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++23
#include <algorithm>
#include <utility>
#include "save.h"

std::default_random_engine dre{};
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	// 여기는 초기화가 아니라 할당하는 작업임.
	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	std::pair<int*, int*> res = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "\n가장 작은 값: " << *(res.first) << " / 가장 큰 값: " << *(res.second) << std::endl;

	save("메인.cpp");
}


==================================================
저장한 시간 - 2026년 03월 16일 15시 01분 36.7161723초
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 10일			(2주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++23
#include <algorithm>
#include <utility>
#include "save.h"

std::default_random_engine dre{};
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	// 여기는 초기화가 아니라 할당하는 작업임.
	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다.
	// std::pair<int*, int*> res = std::minmax_element(std::begin(a), std::end(a));
	auto [작은값, 큰값] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "\n가장 작은 값: " << 작은값 << " / 가장 큰 값: " << 큰값 << std::endl;

	save("메인.cpp");
}


==================================================
저장한 시간 - 2026년 03월 16일 15시 01분 48.2393394초
==================================================

//--------------------------------------------------------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 10일			(2주 2일)
//--------------------------------------------------------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27 (February 2026)
// 프로젝트 설정 - C++언어표준 - /std:c++latest
//				- C/C++언어 - SDL검사 - 아니요
//---------------------------------------------------------------------------------------------------------------------
// 
//----------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>		// C++23
#include <algorithm>
#include <utility>
#include "save.h"

std::default_random_engine dre{};
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	// 여기는 초기화가 아니라 할당하는 작업임.
	for (int& num : a) {
		num = uid(dre);
	}

	for (int num : a) {
		std::print("{:8}", num);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다.
	// std::pair<int*, int*> res = std::minmax_element(std::begin(a), std::end(a));
	auto [작은값, 큰값] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "\n가장 작은 값: " << *작은값 << " / 가장 큰 값: " << *큰값 << std::endl;

	save("메인.cpp");
}
